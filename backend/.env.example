# Required — server will not start if any of these are missing or invalid.
PORT=3000
HOST=0.0.0.0

SQLITE_DATABASE_URL=sqlite:./data/venta_libre.db?mode=rwc

JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRATION_HOURS=24
